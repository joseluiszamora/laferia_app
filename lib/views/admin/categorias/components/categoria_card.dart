import 'package:flutter/material.dart';

class CategoriaCard extends StatelessWidget {
  const CategoriaCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
